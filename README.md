## Compile and run the project

```bash
# install dependencies
npm install
# deploy migrations
npx prisma migrate deploy
# seed the database
npx prisma db seed
# build the project
npm run build
# run the project
npm run start:prod
```